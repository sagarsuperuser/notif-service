apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: notif-api-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod

    # MVP: allow HTTP + HTTPS together
    nginx.ingress.kubernetes.io/ssl-redirect: "false"

    # minimal but useful
    #
    # limit-rps is keyed on the client address, so behind a load balancer that
    # does not preserve the real client IP every caller shares one bucket and
    # the whole ingress is capped at this number. It is left in place because a
    # public endpoint should have a limiter, but it is a per-deployment value:
    # a benchmark measuring the service must raise it, or it measures nginx.
    nginx.ingress.kubernetes.io/limit-rps: "20"
    nginx.ingress.kubernetes.io/proxy-connect-timeout: "5"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "15"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "15"
    nginx.ingress.kubernetes.io/proxy-body-size: "1m"

    # basic auth for API
    nginx.ingress.kubernetes.io/auth-type: basic
    nginx.ingress.kubernetes.io/auth-secret: notif-api-basic-auth
    nginx.ingress.kubernetes.io/auth-realm: "Authentication Required"
spec:
  ingressClassName: nginx

  tls:
    - hosts:
        - api.sagarwaidande.org
      secretName: notif-api-tls

  rules:
    - host: api.sagarwaidande.org
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: notif-api-svc
                port:
                  number: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: notif-webhook-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod

    # MVP: allow HTTP + HTTPS together
    nginx.ingress.kubernetes.io/ssl-redirect: "false"

    # minimal but useful
    #
    # limit-rps is keyed on the client address, so behind a load balancer that
    # does not preserve the real client IP every caller shares one bucket and
    # the whole ingress is capped at this number. It is left in place because a
    # public endpoint should have a limiter, but it is a per-deployment value:
    # a benchmark measuring the service must raise it, or it measures nginx.
    nginx.ingress.kubernetes.io/limit-rps: "20"
    nginx.ingress.kubernetes.io/proxy-connect-timeout: "5"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "15"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "15"
    nginx.ingress.kubernetes.io/proxy-body-size: "1m"
spec:
  ingressClassName: nginx

  tls:
    - hosts:
        - webhook.sagarwaidande.org
      secretName: notif-webhook-tls

  rules:
    - host: webhook.sagarwaidande.org
      http:
        paths:
          # keep only Twilio endpoint exposed
          - path: /v1/webhooks/twilio/status
            pathType: Prefix
            backend:
              service:
                name: notif-webhook-svc
                port:
                  number: 80
