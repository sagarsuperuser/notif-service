controller:
  kind: Deployment

  service:
    type: NodePort
    nodePorts:
      http: 30080
      https: 30443
    externalTrafficPolicy: Cluster

  ingressClassResource:
    name: nginx
    enabled: true
    default: true

  ingressClass: nginx

  admissionWebhooks:
    enabled: true

  metrics:
    enabled: true